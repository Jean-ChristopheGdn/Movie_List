# Site de recherche de films via l'api Movie Data base développé en symphony 

Verifier que Apache est bien lancé ("sudo systemctl enable apache2")

Lancer le serveur symfony : "symfony server:start"
Lancer le projet sur localhost : "symfony open:local"
